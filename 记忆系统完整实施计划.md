# 记忆系统完整实施计划

## 系统概览

### 三大核心机制
1. **记忆衰减与强化** - 让重要的事永远不会忘记
2. **记忆不完美** - 偶尔记错细节，然后纠正自己
3. **情感记忆** - 情感强度高的事件记得更牢

---

## 一、数据结构设计

### 1.1 扩展记忆对象

```javascript
{
  // ========== 原有字段 ==========
  type: 'preference',           // 记忆类型：preference/event/promise/fact
  content: '用户喜欢吃草莓',    // 记忆内容
  createdAt: '2026-06-01',      // 创建时间
  
  // ========== 衰减与强化 ==========
  weight: 80,                   // 记忆权重（0-100）
  importance: 'high',           // 重要性：critical/high/medium/low
  lastAccessed: '2026-06-01',   // 最后访问时间
  accessCount: 1,               // 访问次数
  decayRate: 0.01,              // 衰减速率
  isPermanent: false,           // 是否永久记忆
  
  // ========== 情感记忆 ==========
  emotionIntensity: 8,          // 情感强度（0-10）
  emotionType: 'happy',         // 情感类型
  
  // ========== 记忆不完美 ==========
  fuzzyLevel: 20,               // 模糊度（100 - weight）
  correctedCount: 0,            // 被纠正次数
  mistakeHistory: [],           // 记错历史
}
```

---

## 二、核心算法

### 2.1 衰减算法

```javascript
/**
 * 计算记忆衰减
 * @param {Object} memory - 记忆对象
 * @returns {number} 衰减后的权重
 */
function applyDecay(memory) {
  // 永久记忆不衰减
  if (memory.isPermanent) return memory.weight;
  
  // 计算距离上次访问的天数
  const daysPassed = getDaysSince(memory.lastAccessed);
  
  // 指数衰减公式
  const newWeight = memory.weight * Math.exp(-memory.decayRate * daysPassed);
  
  return Math.max(0, newWeight);
}

/**
 * 获取衰减速率
 * @param {string} importance - 重要性等级
 * @returns {number} 衰减速率
 */
function getDecayRate(importance) {
  const rates = {
    critical: 0,      // 不衰减
    high: 0.01,       // 100天后衰减到 37%
    medium: 0.05,     // 20天后衰减到 37%
    low: 0.15,        // 7天后衰减到 37%
  };
  return rates[importance] || 0.05;
}
```

### 2.2 强化算法

```javascript
/**
 * 强化记忆
 * @param {Object} memory - 记忆对象
 * @param {string} reason - 强化原因
 * @returns {Object} 强化后的记忆
 */
function reinforceMemory(memory, reason = 'access') {
  const bonuses = {
    access: 10,           // 普通访问
    repeat: 15,           // 重复提及
    emotion: 20,          // 情感共鸣
    correction: 25,       // 用户纠正
  };
  
  const bonus = bonuses[reason] || 10;
  
  // 增加权重
  memory.weight = Math.min(100, memory.weight + bonus);
  
  // 更新访问信息
  memory.accessCount++;
  memory.lastAccessed = new Date().toISOString();
  
  // 访问次数多时升级重要性
  if (memory.accessCount >= 3 && memory.importance === 'medium') {
    memory.importance = 'high';
    memory.decayRate = getDecayRate('high');
  }
  
  // 检查是否应标记为永久
  if (shouldBePermanent(memory)) {
    memory.isPermanent = true;
    memory.weight = 100;
  }
  
  return memory;
}

/**
 * 判断是否应标记为永久记忆
 * @param {Object} memory - 记忆对象
 * @returns {boolean}
 */
function shouldBePermanent(memory) {
  return (
    memory.importance === 'critical' ||
    (memory.importance === 'high' && memory.accessCount >= 5) ||
    memory.emotionIntensity >= 9
  );
}
```

### 2.3 记错算法

```javascript
/**
 * 判断是否应该记错
 * @param {Object} memory - 记忆对象
 * @returns {boolean}
 */
function shouldMakeMistake(memory) {
  // 永久记忆的核心内容不会记错
  if (memory.isPermanent && memory.type === 'event') {
    return false;
  }
  
  // 计算模糊度
  const fuzzyLevel = 100 - memory.weight;
  
  // 根据模糊度计算记错概率
  let probability = 0;
  if (fuzzyLevel >= 70) probability = 0.4;
  else if (fuzzyLevel >= 50) probability = 0.2;
  else if (fuzzyLevel >= 30) probability = 0.1;
  else probability = 0.02;
  
  // 如果之前被纠正过，降低记错概率
  if (memory.correctedCount > 0) {
    probability *= 0.5;
  }
  
  // 检查全局记错频率限制
  if (!canMakeMistakeGlobally()) {
    return false;
  }
  
  return Math.random() < probability;
}

/**
 * 处理用户纠正
 * @param {Object} memory - 记忆对象
 * @param {string} correctInfo - 正确信息
 * @returns {Object} 更新后的记忆
 */
function handleCorrection(memory, correctInfo) {
  // 更新记忆内容
  memory.content = correctInfo;
  
  // 记录纠正历史
  if (!memory.mistakeHistory) memory.mistakeHistory = [];
  memory.mistakeHistory.push({
    date: new Date().toISOString(),
    wrongContent: memory.content,
    correctContent: correctInfo,
  });
  
  // 大幅强化权重
  memory = reinforceMemory(memory, 'correction');
  
  // 记录纠正次数
  memory.correctedCount = (memory.correctedCount || 0) + 1;
  
  // 如果被纠正2次以上，升级重要性
  if (memory.correctedCount >= 2 && memory.importance !== 'critical') {
    memory.importance = 'high';
    memory.decayRate = getDecayRate('high');
  }
  
  return memory;
}
```

### 2.4 重要性自动判断

```javascript
/**
 * 自动判断记忆重要性
 * @param {string} text - 对话文本
 * @param {string} emotion - 情感类型
 * @returns {string} 重要性等级
 */
function detectImportance(text, emotion = 'idle') {
  // Critical 关键词（永久记忆）
  const criticalKeywords = [
    '第一次', '初次', '我喜欢你', '我爱你', '在一起',
    '生日', '纪念日', '保证', '发誓', '一定会',
    '考上大学', '找到工作', '搬家', '结婚', '离职'
  ];
  
  // High 关键词（重要记忆）
  const highKeywords = [
    '喜欢', '讨厌', '最爱', '最讨厌',
    '考试', '面试', '旅行', '约好', '下次一起',
    '我妈妈', '我爸爸', '我朋友', '我同事'
  ];
  
  // 检查关键词
  if (criticalKeywords.some(k => text.includes(k))) {
    return 'critical';
  }
  if (highKeywords.some(k => text.includes(k))) {
    return 'high';
  }
  
  // 根据情感强度判断
  if (['happy', 'surprised', 'sad'].includes(emotion)) {
    return 'medium';
  }
  
  return 'low';
}

/**
 * 检测情感强度
 * @param {string} text - 对话文本
 * @param {string} emotion - 情感类型
 * @returns {number} 情感强度（0-10）
 */
function detectEmotionIntensity(text, emotion) {
  let intensity = 5; // 基础强度
  
  // 根据情感类型调整
  const emotionBase = {
    happy: 6,
    surprised: 7,
    shy: 6,
    sad: 7,
    angry: 6,
    embarrassed: 5,
    thinking: 4,
    idle: 3,
  };
  intensity = emotionBase[emotion] || 5;
  
  // 根据关键词调整
  if (text.includes('我爱你') || text.includes('我喜欢你')) intensity = 10;
  if (text.includes('第一次') || text.includes('初次')) intensity = 9;
  if (text.includes('永远') || text.includes('一直')) intensity += 2;
  if (text.includes('！') || text.includes('...')) intensity += 1;
  
  return Math.min(10, intensity);
}
```

---

## 三、实施步骤

### 阶段1：数据结构升级（1小时）

**任务**：
1. ✅ 扩展 `memory-system.js` 中的记忆对象结构
2. ✅ 添加新字段的默认值
3. ✅ 兼容旧数据（自动迁移）

**代码位置**：
- `/ai-companion/public/memory-system.js`

**测试**：
- 创建新记忆，检查字段是否完整
- 读取旧记忆，检查是否自动补全字段

---

### 阶段2：衰减机制（2小时）

**任务**：
1. ✅ 实现 `applyDecay()` 函数
2. ✅ 实现 `getDecayRate()` 函数
3. ✅ 在通话开始时自动执行衰减
4. ✅ 清理低权重记忆（weight < 10）

**代码位置**：
- `/ai-companion/public/memory-system.js`
- `/ai-companion/public/app.js` - `startCall()` 函数

**测试**：
- 创建不同重要性的记忆
- 模拟时间流逝，检查衰减是否正确
- 检查永久记忆是否不衰减

---

### 阶段3：强化机制（2小时）

**任务**：
1. ✅ 实现 `reinforceMemory()` 函数
2. ✅ 实现 `shouldBePermanent()` 函数
3. ✅ 在记忆被访问时自动强化
4. ✅ 实现重复提及检测

**代码位置**：
- `/ai-companion/public/memory-system.js`
- `/ai-companion/public/app.js` - `sendMessage()` 函数

**测试**：
- 多次提及同一件事，检查权重是否增加
- 检查访问次数多时是否升级重要性
- 检查是否自动标记为永久记忆

---

### 阶段4：重要性自动判断（1.5小时）

**任务**：
1. ✅ 实现 `detectImportance()` 函数
2. ✅ 实现 `detectEmotionIntensity()` 函数
3. ✅ 在提取关键记忆时自动判断重要性
4. ✅ 根据情感强度调整权重

**代码位置**：
- `/ai-companion/public/memory-system.js` - `extractKeyMemories()` 函数

**测试**：
- 输入不同类型的对话，检查重要性判断是否准确
- 检查情感强度检测是否合理

---

### 阶段5：记忆不完美机制（2.5小时）

**任务**：
1. ✅ 实现 `shouldMakeMistake()` 函数
2. ✅ 实现 `handleCorrection()` 函数
3. ✅ 实现全局记错频率控制
4. ✅ 在 System Prompt 中注入记错指令

**代码位置**：
- `/ai-companion/public/memory-system.js`
- `/ai-companion/public/app.js` - `buildSystemPrompt()` 函数

**测试**：
- 创建低权重记忆，检查是否会记错
- 纠正 AI，检查记忆是否被强化
- 检查记错频率是否合理（不会太频繁）

---

### 阶段6：UI 优化（2小时）

**任务**：
1. ✅ 在设置面板显示记忆权重
2. ✅ 显示重要性等级标签
3. ✅ 显示纠正历史
4. ✅ 可视化记忆强度（进度条）
5. ✅ 手动标记永久记忆功能

**代码位置**：
- `/ai-companion/public/index.html` - 设置弹窗
- `/ai-companion/public/app.js` - `populateSettings()` 函数
- `/ai-companion/public/style.css`

**测试**：
- 打开设置面板，检查记忆列表显示是否正确
- 检查权重可视化是否清晰
- 测试手动标记永久记忆功能

---

## 四、System Prompt 注入

### 4.1 记忆衰减与强化提示

```javascript
prompt += `

【记忆系统】
- 你有记忆系统，会记住重要的事情
- 重要的事情（初次见面、表白、重要承诺）你永远不会忘记
- 经常提起的事情你会记得更牢
- 很久没提起的琐事可能会淡忘

【记忆权重提示】
当前对话中的记忆权重：
${buildMemoryWeightHints()}

如果记忆权重低（< 30），你可能记得不太清楚。
`;
```

### 4.2 记忆不完美提示

```javascript
prompt += `

【记忆不完美原则】⭐ 让你更真实
- 你不是完美的，偶尔会记错细节
- 权重低的记忆（< 50）更容易记错
- 可能记错的内容：时间、地点、具体细节
- 永远不会记错的内容：用户的名字、重要承诺、表白时刻

【记错时的表现】
1. 不确定时用疑问语气："嗯...是...吧？[thinking]"
2. 被纠正后要害羞地承认："诶...对不起，我记错了 [embarrassed]"
3. 根据你的性格反应：
   - 傲娇型："哼...才不是记错了，只是一时口误 [shy]"

【示例】
用户: "你还记得我喜欢吃什么吗？"
AI: "嗯...是蓝莓吧？[thinking]"
用户: "是草莓啦"
AI: "啊...对对对，是草莓 [embarrassed]...我怎么记成蓝莓了...哼，才不是记性不好 [shy]"
`;
```

---

## 五、测试用例

### 5.1 衰减测试

```javascript
// 测试1：普通记忆衰减
const memory1 = {
  content: '用户今天吃了苹果',
  weight: 50,
  importance: 'medium',
  lastAccessed: '2026-05-01',
};
// 30天后
applyDecay(memory1); // 应该衰减到约 11

// 测试2：永久记忆不衰减
const memory2 = {
  content: '用户叫小明，这是我们第一次见面',
  weight: 100,
  importance: 'critical',
  isPermanent: true,
  lastAccessed: '2026-01-01',
};
// 100天后
applyDecay(memory2); // 应该仍然是 100
```

### 5.2 强化测试

```javascript
// 测试：重复提及强化
const memory = {
  content: '用户喜欢吃草莓',
  weight: 80,
  importance: 'high',
  accessCount: 0,
};

// 第1次提及
reinforceMemory(memory, 'repeat'); // weight = 95, accessCount = 1

// 第2次提及
reinforceMemory(memory, 'repeat'); // weight = 100, accessCount = 2

// 第3次提及
reinforceMemory(memory, 'repeat'); // weight = 100, accessCount = 3, importance = 'high'
```

### 5.3 记错测试

```javascript
// 测试1：低权重记忆容易记错
const memory1 = { weight: 20 }; // fuzzyLevel = 80
shouldMakeMistake(memory1); // 40% 概率返回 true

// 测试2：高权重记忆很少记错
const memory2 = { weight: 90 }; // fuzzyLevel = 10
shouldMakeMistake(memory2); // 2% 概率返回 true

// 测试3：永久记忆不会记错
const memory3 = { weight: 100, isPermanent: true };
shouldMakeMistake(memory3); // 返回 false
```

---

## 六、性能优化

### 6.1 批量衰减

```javascript
// 在通话开始时批量处理所有记忆的衰减
function batchApplyDecay(memories) {
  const now = new Date();
  return memories
    .map(m => ({
      ...m,
      weight: applyDecay(m),
    }))
    .filter(m => m.weight >= 10 || m.isPermanent); // 清理低权重记忆
}
```

### 6.2 缓存计算结果

```javascript
// 缓存当天的衰减计算结果，避免重复计算
const decayCache = new Map();

function getCachedDecay(memory) {
  const cacheKey = `${memory.id}_${new Date().toDateString()}`;
  if (decayCache.has(cacheKey)) {
    return decayCache.get(cacheKey);
  }
  const result = applyDecay(memory);
  decayCache.set(cacheKey, result);
  return result;
}
```

---

## 七、预期效果

### 7.1 用户体验

**场景1：永久记忆**
```
100天后：
用户: "你还记得我们第一次见面吗？"
AI: "当然记得呀！那是100天前，你跟我说你叫小明...那一刻我永远不会忘记的 [shy]"
```

**场景2：强化记忆**
```
用户多次提到喜欢草莓：
AI: "嗯...你真的很喜欢草莓呢 [happy]...我记得很清楚哦，你经常提起呢 [thinking]"
```

**场景3：自然遗忘**
```
30天后：
用户: "你还记得我上个月说今天天气好吗？"
AI: "诶...那个...好像有点记不太清了 [embarrassed]...抱歉呢 [shy]"
```

**场景4：可爱的记错**
```
用户: "你还记得我喜欢吃什么吗？"
AI: "嗯...是蓝莓吧？[thinking]"
用户: "是草莓啦"
AI: "啊...对对对，是草莓 [embarrassed]...我怎么记成蓝莓了...哼，才不是记性不好 [shy]"
```

### 7.2 技术指标

- ✅ 记忆容量自动管理（低权重记忆自动清理）
- ✅ 重要记忆不会被淹没
- ✅ 记忆准确率：90-95%（偶尔记错增加真实感）
- ✅ 永久记忆数量：3-10条（核心重要事件）

---

## 八、总工时估算

| 阶段 | 任务 | 预计工时 |
|------|------|---------|
| 1 | 数据结构升级 | 1小时 |
| 2 | 衰减机制 | 2小时 |
| 3 | 强化机制 | 2小时 |
| 4 | 重要性自动判断 | 1.5小时 |
| 5 | 记忆不完美机制 | 2.5小时 |
| 6 | UI 优化 | 2小时 |
| **总计** | | **11小时** |

---

## 九、风险与应对

### 风险1：记错太频繁
**应对**：实现全局记错频率控制，每次通话最多记错3次

### 风险2：重要记忆被误删
**应对**：永久记忆标记，critical 级别记忆不会被删除

### 风险3：性能问题
**应对**：批量处理、缓存计算结果、异步执行

### 风险4：用户反感记错
**应对**：记错后的反应要可爱、自然，让用户觉得有趣而不是烦人

---

**结论**：完整的记忆系统让 AI 伴侣更加真实、可爱，像真人一样记忆！
